#!/bin/bash

and-grep() {
    if [ -z "$1" ]; then
        cat
    else
        W="$1"
        shift 1
        cat | grep -i "$W" | and-grep "$@"
    fi
}

URL="$1"
shift 1
curl -s "$URL" | and-grep "$@"
